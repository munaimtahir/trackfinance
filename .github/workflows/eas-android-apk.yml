name: Build Android APK with Expo EAS

on:
  workflow_dispatch: {}  # run manually from the Actions tab
  push:
    branches:
      - main
      - 'copilot/**'  # Allow testing on copilot branches

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      # Expo authentication (GitHub Secrets)
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      # Expo / EAS project UUID (GitHub Secret)
      EAS_PROJECT_ID: ${{ secrets.EAS_PROJECT_ID }}

      # Firebase configuration (GitHub Secrets)
      EXPO_PUBLIC_FIREBASE_API_KEY: ${{ secrets.EXPO_PUBLIC_FIREBASE_API_KEY }}
      EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN }}
      EXPO_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.EXPO_PUBLIC_FIREBASE_PROJECT_ID }}
      EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET }}
      EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
      EXPO_PUBLIC_FIREBASE_APP_ID: ${{ secrets.EXPO_PUBLIC_FIREBASE_APP_ID }}

    steps:
      # 1) Check out the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2) Set up Node.js
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: trackfinance-clean/package-lock.json

      # 3) Install dependencies and EAS CLI
      - name: Install dependencies
        working-directory: trackfinance-clean
        run: |
          npm ci || npm install
          npm install -g eas-cli

      # 4) (Optional) Show public Expo config for debugging
      - name: Show Expo public config
        working-directory: trackfinance-clean
        run: npx expo config --type public

      # 5) Verify Expo authentication
      - name: Verify Expo authentication
        run: npx eas whoami

      # 6) Build Android APK on Expo EAS and download it
      - name: Build Android APK (preview profile)
        working-directory: trackfinance-clean
        run: |
          # Start build and wait for completion
          npx eas build --platform android --profile preview --non-interactive --wait

      # 7) Download and upload APK as artifact
      - name: Download and upload APK
        working-directory: trackfinance-clean
        run: |
          # Get the latest build artifact URL
          BUILD_URL=$(npx eas build:list --platform android --limit 1 --json --non-interactive | jq -r '.[0].artifacts.buildUrl')
          echo "Build URL: $BUILD_URL"
          
          # Download the APK
          if [ -n "$BUILD_URL" ] && [ "$BUILD_URL" != "null" ]; then
            curl -L -o trackfinance.apk "$BUILD_URL"
          else
            echo "Warning: Could not get build URL"
          fi

      - name: Upload APK as GitHub artifact
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: trackfinance-apk
          path: trackfinance-clean/trackfinance.apk
          if-no-files-found: warn
